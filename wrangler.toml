name = "sunball-finder"
main = "functions/search.js"
compatibility_date = "2024-01-01"

# For development - use Cloudflare Pages for the static site
# The Worker handles /api/* routes

[vars]
# Add API keys as secrets via `wrangler secret put`
# GOOGLE_MAPS_KEY = ""

# Deploy with:
# wrangler pages deploy . --project-name sunball-finder
# Or for the worker API:
# wrangler deploy
